# Tanner

I build automations, internal tools and desktop applications, and hand over the
finished thing — software that runs, with a written record of what was actually
verified rather than claimed.

Available for contract work. Fixed price, written updates, source included.

---

## Shipped

| Project | What it is | The measurement that proves it |
| --- | --- | --- |
| **[Pulse](https://github.com/Xer0z-gh/Pulse)** | Genre-smart music discovery over internet radio. Builds its own station database by listening to what stations broadcast instead of trusting directory tags. | 133,488 observed plays across 15,742 stations, producing **38,335 genre links no station ever claimed**. |
| **[Launch Deck](https://github.com/Xer0z-gh/Launch-Deck)** | A local-first control centre that detects what a project is, works out how to start it, then runs, supervises and stops it. Process supervision on Windows Job Objects. | **353 tests across 14 suites.** Reports 56 registered projects with 16 blocked, each carrying its own repair step. |
| **[VideoForge](https://github.com/Xer0z-gh/Video-Forge)** | One command turns a brief into a rendered, captioned, loudness-normalized MP4 with thumbnail, titles and chapters. | Every edit is a **typed, invertible, journaled operation** — undo, version history and transcript-based editing are structural, not bolted on. |
| **[Vesper](https://github.com/Xer0z-gh/Vesper)** | A 13-module creative sound design environment: reorderable effects rack, 17 saturation algorithms, linear-phase match EQ, convolution reverb. VST3 and standalone. | Built on JUCE against a **written design system**, with host-matrix QA. |
| **[Coupon Hunter](https://github.com/Xer0z-gh/Coupon-Hunter)** | Browser extension that finds working checkout codes, applies each candidate, watches the order total react, and keeps the best one. | **~20 sources queried in parallel**, answered at a 1.2 s cutoff; each attempt ends on the merchant's response, not a fixed timer. |

Stack across these: **Rust** (Tauri, Axum), **Python**, **TypeScript/React**,
**C++20**, **C#/.NET**. Windows-first, local-first, no telemetry.

---

## How a job runs

1. **You describe the outcome** — not a spec, the thing you want to be true when it's done.
2. **A small paid trial task**, scoped to a few hours, with a pass/fail you can check yourself.
3. **Fixed quote for the real work**, agreed before I start. The number doesn't move unless the scope does.
4. **You get software that runs, plus its source**, and a written handover covering what was tested and what was not.

---

## The part most people don't put in writing

I direct AI tooling, and I verify everything it produces. You're paying for a
working deliverable and for the judgement that gets it there — scoping the
problem, choosing the approach, and proving the result before it reaches you.
That's why the numbers above exist: each one comes from a test run or live
instrumentation, because a claim nobody exercised is not a result.

What I don't do: hourly staff-augmentation, live technical interviews, or
claiming code was written unaided.

---

**Written enquiries:** tanner8206@gmail.com

<sub>Source in these repositories is published for review. It is not licensed for reuse.</sub>
